if(page==='lesson'){$('#open-syllabus').onclick=()=>{$('#open-syllabus').style.display='none';$('#syllabus-reveal').classList.add('show');setTimeout(unlock,450)}}
if(page==='quiz'){const no=$('#no-date'),area=$('#date-actions'),comment=$('#no-comment');let escapes=0;const lines=['Too slow 😌','Wrong button, Bokya.','Nice try. It moved again.','The “No” button is afraid of commitment.','Still trying? Impressive.','Okay, the button has left the country. ✈'];function escapeNo(event){if(event)event.preventDefault();escapes+=1;comment.textContent=lines[Math.min(escapes-1,lines.length-1)];if(escapes>=6){no.classList.add('vanishing');return}const maxX=Math.max(0,area.clientWidth-no.offsetWidth-8),maxY=Math.max(0,area.clientHeight-no.offsetHeight-8);no.style.left=Math.round(Math.random()*maxX)+'px';no.style.top=Math.round(Math.random()*maxY)+'px'}no.addEventListener('pointerenter',escapeNo);no.addEventListener('pointerdown',escapeNo);no.addEventListener('focus',escapeNo);no.addEventListener('click',escapeNo);$('#yes-date').onclick=()=>{$('#proposal').style.display='none';$('#accepted').classList.add('show');celebrate(90);setTimeout(unlock,500)}}
if(page==='ticket'){$('#tear').onclick=()=>{$('#tear').textContent='Date plan unsealed · Boku must pack soon ✈';celebrate(30);unlock()}}
